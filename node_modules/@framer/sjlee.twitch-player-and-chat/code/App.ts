import { Data, animate, Override, Animatable } from "framer"

const data = Data({ 
    background: "https://clips-media-assets2.twitch.tv/226318388.mp4"
})

export const addVideo: Override = () => {
    return {
        video: data.background,
    }
}
